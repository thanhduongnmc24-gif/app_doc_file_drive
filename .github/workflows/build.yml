name: Build Android APK Release

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout mã nguồn
        uses: actions/checkout@v4

      - name: Khởi tạo Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Cài đặt Java JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Cài đặt Android SDK
        uses: android-actions/setup-android@v3

      - name: Cài đặt các thư viện phụ thuộc
        run: npm install

      - name: Thực hiện Expo Prebuild cấu trúc Android Native
        run: npx expo prebuild --platform android

      - name: Biên dịch tạo file APK Release
        run: |
          cd android
          ./gradlew assembleRelease

      - name: Đóng gói và upload file APK lên Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Ung-Dung-Doc-Truyen-Cua-Anh-Hai
          path: android/app/build/outputs/apk/release/app-release.apk